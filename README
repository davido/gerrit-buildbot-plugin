Gerrit Tinderbox Integration Plugin

INSTALL:
=======

Prerequisites:
==============
Currently you must compile and install the main gerrit repo manually.
cd <gerrit-main-repo-home> && mvn install

Compilation:
============
mvn package

Installation:
=============
cp target/buildbot-version.jar <gerit_site>/plugins

Version:
========
Developed and tested against gerrit-2.5-SHAPSHOT
